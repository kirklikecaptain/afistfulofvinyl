type ArtistColors {
  lightest: String!
  light: String!
  mid: String!
  dark: String!
  darkest: String!
}

extend type Artist {
  pagePath: String!
  colors: ArtistColors!
}

extend type Video {
  pagePath: String!
}
