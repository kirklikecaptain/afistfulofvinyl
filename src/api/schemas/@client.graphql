type ArtistColors {
  lightest: String!
  light: String!
  mid: String!
  dark: String!
  darkest: String!
}

type Artist {
  pagePath: String!
  colors: ArtistColors!
}

type Video {
  pagePath: String!
}