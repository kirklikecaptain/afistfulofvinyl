export function Divider() {
  return (
    <hr
      style={{
        margin: "1.25rem 0",
        border: "none",
        borderBottom: "1px solid var(--gray-5)",
        width: "100%",
      }}
    />
  );
}
